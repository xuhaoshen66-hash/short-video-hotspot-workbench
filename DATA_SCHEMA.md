# 热点数据字段说明

热点内容现在统一维护在 `data.js` 里，`app.js` 只负责页面交互。

每条热点建议包含这些字段：

- `id`：唯一标识，英文短横线命名，例如 `bank-rate`。
- `title`：热点标题。
- `category`：分类，目前支持 `金融`、`科技`、`民生`、`AI`、`教育`。
- `platforms`：来源平台数组，例如 `["微博", "百度"]`。
- `heat`：综合热度分。
- `viral`：爆款潜力值。
- `videoHeat`：视频化热度值。
- `firstSeen`：首次出现时间。
- `trend`：趋势，支持 `上升`、`持平`、`下降`。
- `rangeScore`：日榜、三天榜、周榜、月榜分数。
- `summary`：短摘要，用于兜底展示。
- `listDescription`：首页排行榜详细描述，建议 100-200 字。
- `detailContent`：详情页客观事件详情，可分段，用 `\n\n` 分隔。
- `why`：为什么会火。
- `risk`：风险提醒。
- `hotComments`：最火辣评列表。
- `images`：剪辑配图建议。
- `angles`：拍摄角度建议。

## 时间线数据

收藏示例时间线现在维护在 `timelines.js`，由 `window.SEED_TIMELINES` 提供。

每条时间线对应一个热点 `id`：

- `id`：必须和 `data.js` 里的热点 `id` 一致。
- `status`：进入选题库后的默认状态。
- `timeline`：时间线节点数组。

时间线节点建议只记录重大转折，不记录普通热度波动。

节点常用字段：

- `time`：节点时间。
- `source`：来源平台或信息来源。
- `stage`：节点阶段，例如 `起点`、`转折`、`当前`。
- `level`：重要程度，例如 `普通`、`重要`、`关键`。
- `title`：节点标题。
- `text`：节点说明，讲清发生了什么和为什么重要。
- `people`：可选，只有名人、官方、媒体或关键主体发声时填写。
- `quote`：可选，只有明确原话、金句或高传播表达时填写。
- `impact`：对短视频创作的判断。
- `linksByType`：按 `新闻`、`官方`、`讨论` 分类的搜索或来源链接。
